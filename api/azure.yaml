# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: api-containerapp
metadata:
  template: monorepo-templates-api@1.0.0
services:
  api:
    project: ./src
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
      remoteBuild: true
